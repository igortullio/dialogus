---
status: completed
title: Add @dialogus/shared/schemas/ingestion + problem slugs
type: backend
complexity: low
dependencies: []
---

# Task 1: Add @dialogus/shared/schemas/ingestion + problem slugs

## Overview

Add the shared Zod schemas that describe ingestion status, progress, and chunk-read DTOs, and extend the `problem` middleware slug registry with the seven new ingestion-related error types. These are the wire-contract primitives that the routes in task_14 emit and the worker handlers in tasks 10-13 consume.

<critical>
- ALWAYS READ the PRD and TechSpec before starting
- REFERENCE TECHSPEC for implementation details — do not duplicate here
- FOCUS ON "WHAT" — describe what needs to be accomplished, not how
- MINIMIZE CODE — show code only to illustrate current structure or problem areas
- TESTS REQUIRED — every task MUST include tests in deliverables
</critical>

<requirements>
- MUST create `packages/shared/src/schemas/ingestion.ts` exposing Zod schemas (and inferred types) per Feature 002 TechSpec § API Endpoints: `IngestionStatus` enum literal set, `IngestionStatusDto`, `ChunkReadDto`, and `IngestionEnqueueResponseDto`.
- MUST add problem slugs listed in Feature 002 TechSpec § API Endpoints → "Error slugs added": `book-not-in-discovered-state`, `book-not-in-retryable-state`, `book-already-ready`, `ingestion-download-failed`, `ingestion-parse-failed`, `ingestion-embed-failed`, `chunk-not-found`.
- MUST extend the catalog `problem` middleware's error-class→slug mapping (from Feature 001 task_11) so that downstream tasks can throw domain errors and get correct Problem Details responses.
- MUST add the new slugs to the README "API Problems" section inventory (file is touched, full section regenerated by task_18 closure).
- MUST extend `@dialogus/shared/package.json` `exports` map with `./schemas/ingestion` entry.

</requirements>

## Subtasks

- [x] 1.1 Author `packages/shared/src/schemas/ingestion.ts` with all DTOs.
- [x] 1.2 Extend the slug mapping in `apps/api/src/infrastructure/http/middleware/problem.ts` (or equivalent shared registry).
- [x] 1.3 Extend `@dialogus/shared` exports map and root barrel.
- [x] 1.4 Update README "API Problems" section to list the new 7 slugs with their default HTTP status.
- [x] 1.5 Unit tests for each schema (happy + failure paths).

## Implementation Details

Reference Feature 002 TechSpec § API Endpoints for the exact Zod field list on each DTO, and § Error slugs for the status-code mapping. Schemas use `.strip()` mode (tolerant to unknown fields, consistent with catalog precedent).

### Relevant Files

- Feature 002 TechSpec § API Endpoints.
- `packages/shared/src/schemas/library.ts` (from 001-catalog task_03) — pattern reference.
- `apps/api/src/infrastructure/http/middleware/problem.ts` (from 001-catalog task_11).

### Dependent Files

- `packages/shared/src/schemas/ingestion.ts` (new)
- `packages/shared/src/schemas/index.ts` (modify barrel)
- `packages/shared/src/index.ts` (modify barrel)
- `packages/shared/package.json` (modify exports)
- `apps/api/src/infrastructure/http/middleware/problem.ts` (modify: add 7 slug mappings)
- `README.md` (modify: API Problems section)
- `packages/shared/__tests__/schemas/ingestion.test.ts` (new)

## Deliverables

- `IngestionStatusDto`, `ChunkReadDto`, `IngestionEnqueueResponseDto` exported from `@dialogus/shared/schemas/ingestion`.
- 7 new slug mappings in `problem` middleware.
- Updated README section.
- Unit tests with 80%+ coverage **(REQUIRED)**.
- Integration tests **(REQUIRED)** — covered by task_14 route tests and task_16 integration suites.

## Tests

- Unit tests:
  - [x] `IngestionStatusDto.parse(validDto)` succeeds for status=`discovered` with `stage: null` and `progress: 0`.
  - [x] `IngestionStatusDto.parse(validDto)` succeeds for status=`embedding` with `stage: 'embed'` and `progress: 34`.
  - [x] `IngestionStatusDto.parse({ status: 'bogus' })` fails — invalid enum.
  - [x] `IngestionStatusDto.parse(validDto with error)` shapes the error object correctly.
  - [x] `ChunkReadDto.parse(validChunk)` succeeds; rejects when `text` is missing.
  - [x] Slug map contains all 7 new slugs with correct default status codes.
- Integration tests:
  - [ ] Deferred to task_14 / task_16.
- Test coverage target: >=80%
- All tests must pass

## Success Criteria

- All tests passing
- Test coverage >=80%
- Downstream tasks (task_10-15, task_14) can import from `@dialogus/shared/schemas/ingestion` without circular-dep issues.
- `problem` middleware returns correct status + slug for each thrown error class.
